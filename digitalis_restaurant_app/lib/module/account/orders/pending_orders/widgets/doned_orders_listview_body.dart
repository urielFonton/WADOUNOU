import 'package:flutter/material.dart';

class DonedOrdersListviewBody extends StatelessWidget {
  const DonedOrdersListviewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('Terminée'),));
  }
}