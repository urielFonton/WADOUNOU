import 'package:flutter/material.dart';

class DonedReservationListviewBody extends StatelessWidget {
  const DonedReservationListviewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Terminée(s)"),
      ),
    );
  }
}