import 'package:flutter/material.dart';

class WaitingReservationListviewBody extends StatelessWidget {
  const WaitingReservationListviewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("En Cours"),
      ),
    );
  }
}